<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Android 17 blocks local network traffic unless the user grants this
      runtime permission. A self-hosted Mealie instance usually lives on the
      LAN, so Umai asks for it - and only asks when the configured address
      really resolves to a local address.
    -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <!-- A cooking timer that reaches zero may vibrate, if the reader allows it in the settings. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      Cooking timers work as in a timer app: they keep running once the cooking mode or the
      app is left, and must ring on time.
       * POST_NOTIFICATIONS shows them outside the app; asked for when the first one starts.
       * FOREGROUND_SERVICE* keeps the app running while a timer exists (CookingTimerService).
       * USE_EXACT_ALARM wakes the device up when a timer ends, even in Doze: timers are a core
         feature, and this permission is granted at install, unlike SCHEDULE_EXACT_ALARM.
       * WAKE_LOCK keeps the device awake while the alarm rings, then lets it sleep again.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".UmaiApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Umai.Starting">

        <!--
          The app is designed for portrait only, on purpose: its layouts are not made for landscape.
          LockedOrientationActivity and DiscouragedApi are ignored for that reason; Android itself
          lifts the lock on large screens (600dp and wider), where the layouts still work.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Umai.Starting"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="LockedOrientationActivity,DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- "Share -> Umai" from a browser or a recipe app opens the import with the page's address. -->
            <intent-filter android:label="@string/share_import_label">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Keeps the cooking timers alive while the app is in the background. -->
        <service
            android:name=".cooking.data.CookingTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Cooking timers that keep counting down and ring when they reach zero, as in a timer app." />
        </service>

        <!-- The wake-up alarm of the next timer and the actions of the timer notifications. -->
        <receiver
            android:name=".cooking.data.TimerActionReceiver"
            android:exported="false" />

        <!-- Lets the camera app write the photo Umai asked for (CameraCapture). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.images"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/image_paths" />
        </provider>
    </application>

</manifest>
